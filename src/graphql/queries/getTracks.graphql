#import BaseTrack from '../fragments/BaseTrack.graphql'

query getTracks($limit: Int!, $after: ID) {
	getTracks(limit: $limit, after: $after) {
		edges {
			cursor
			node {
				...BaseTrack
			}
		}
		pageInfo {
			hasPreviousPage
			hasNextPage
		}
	}
}
